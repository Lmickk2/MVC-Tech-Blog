{{#each users}}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{Post.name}}</h2>
  </div>
  <div class="col-md-6">
    <h3>{{Post.needed_funding}}</h3>
  </div>
</div>
{{/each}}